<% include partials/global/header %>

    <% include partials/global/top.ejs %>
    <main class="detail">
        <article>
            <h1 class="title"><%= article.storyTitle %></h1>
            <div class="iconWrapper">
                <div class="icon">
                    <i class="fa fa-heart like" aria-hidden="true"></i><span class="amountOfLikes">0 Likes</span>
                </div>
                <div class="icon">
                    <i class="fa fa-mobile" aria-hidden="true"></i><span><%= article.category %></span>
                </div>
                <div class="icon">
                    <i class="fa fa-pencil" aria-hidden="true"></i></i><span><%= article.writer %></span>
                </div>
            </div>
            <img class="image" src="<%= article.image %>">
            <p><%= article.storyTextPart1 %></p>
            <section class="cta">
                <div>Moeten anderen dit ook lezen?</div>
                <input class="readLater" type="button" name="like" value="Iedereen moet dit lezen!">
            </section>
            <p><%= article.storyTextPart2 %></p>
            <blockquote><%= article.storyQuote %></blockquote>
            <p><%= article.storyTextPart3 %></p>
            <p><%= article.storyTextPart4 %></p>
            <p><%= article.storyTextPart5 %></p>
            <p class="hide link"><%= article.id %></p>

            <section class="cta cta-like">
                <div>Wat vond je van dit artikel?</div>
                <input type="button" class="likeArticle" value="Like">
                <input type="button" class="dislikeArticle" value="Dislike">
            </section>
        </article>

        <section>
            <h2 class="hide">Bekijk ook deze artikelen</h2>
            <div class="suggest"></div>
        </section>
    </main>

<% include partials/global/footer.ejs %>
