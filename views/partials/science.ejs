    <% storydata.science.forEach(function (story) {%>
        <div class="science-story hide">
            <h1><%= story.storyTitle %></h1>
            <p><strong><%= story.storyIntro %></strong></p>
        </div>
    <% }); %>
